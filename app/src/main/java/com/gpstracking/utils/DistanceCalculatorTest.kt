package com.gpstracking.utils

import com.gpstracking.Room.LocationPointEntity

class DistanceCalculatorTest {

 /*   @Test
    fun distance_isCalculatedCorrectly() {
        val points = listOf(
            LocationPointEntity(0, 1, 12.0, 77.0, 0),
            LocationPointEntity(0, 1, 12.0005, 77.0005, 1000)
        )

        val distance = calculateDistance(points)
        assertTrue(distance > 0)
    }*/
}
